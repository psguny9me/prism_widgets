// Enum for MATERIAL effects (under the image)
enum MaterialEffect {
  none,
  chromeMetal,
  diamondPrism,
}

// Enum for COATING effects (over the image)
enum CoatingEffect {
  none,
  prism,
  sparkle,
}
